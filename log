# Supprimer les certificats en échec
sudo rm -rf /var/lib/caddy/.local/share/caddy/certificates/

# Redémarrer Caddy
sudo systemctl restart caddy

# Voir l'obtention des certificats en temps réel
sudo journalctl -u caddy -f --no-pager | grep -E "beartify|certificate|acme"
